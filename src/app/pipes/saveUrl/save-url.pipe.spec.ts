import { SaveUrlPipe } from './save-url.pipe';

describe('SaveUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SaveUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
